# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# NOTE: public/data and public/logos are COMMITTED (not ignored) so the Vercel
# deploy is self-contained — Vercel's build root is web/ and doesn't include the
# repo-root data files. `npm run sync` refreshes them; re-commit after data edits.
