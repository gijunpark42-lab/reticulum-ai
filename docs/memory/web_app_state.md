---
name: web-app-state
description: "Next.js web app on Vercel (live), deploy recipe, sync rule, no-Quant-tab decision, Generations tab"
metadata: 
  node_type: memory
  type: project
  originSessionId: f1dc05aa-234e-4e23-a8c9-a52cb8b3388a
  modified: 2026-09-11T23:39:36.348Z
---

**The frontend is now a Next.js app in `web/`, LIVE on Vercel (since 2026-07-01); Streamlit is DELETED from main** (backup: tag `streamlit-v1-pre-vercel`, branch `backup/streamlit-pre-vercel`). `graph_build.py` / `taxonomy.py` / `quant/event_study.py` are KEPT (pipeline + yfinance user).

- **Stack:** Next.js 14 + TypeScript, plain CSS w/ `--ap-*` design tokens (no Tailwind), `react-force-graph-3d` (three pinned 0.185 via package overrides — two three.js copies froze OrbitControls once). Views: Graph / Chain 2D / Generations / Timelines / Screener / Capex / Coverage, overlay NodePanel; libs in `web/src/lib/` (taxonomy, signals, data, chain2d, transitions).
- **Capex tab (2026-08-05):** `CapexBacklog.tsx` renders hand-curated `capex_backlog.json` (repo root, synced to web/public/data) — KPI tiles + two single-hue bar charts (capex #2997ff, backlog #2ea852, dataviz-validated) + per-group tables, hyperscalers & neoclouds, every figure source-labeled. Since 2026-08-27 the served file is `graph/capex_backlog.json`, DERIVED by `derive.py` from `capex`-tagged graph entries on top of the curated baseline (root `capex_backlog.json` = fallback, never auto-written) — tag hyperscaler/neocloud entries instead of editing the file. Windows gotcha: after `npm run build`, delete `web/.next` before `npm run dev` (OneDrive readlink EINVAL).
- **Mobile (2026-08-27):** responsive pass in `globals.css` (one `@media (max-width: 860px)` block + a `≤400px` one) — sidebar becomes an off-canvas drawer behind a `☰ Filters` button (`navOpen` in `page.tsx`, `open`/`onClose` props on `Sidebar`), the 7-tab segmented control becomes a scroll-snap strip that auto-scrolls the selected tab into view (no `behavior:'smooth'` — with scroll-snap Chrome silently no-ops it), graph/Chain-2D sized in `dvh`, NodePanel becomes a full-screen sheet. `layout.tsx` exports `viewport` (`viewportFit:'cover'` → `env(safe-area-inset-*)`). Wide tables card-stack at ≤700px via `:is(.data.screener, .data.tl)` + `data-label`; `.tbl-wrap:has(.data.screener)` keeps plain tables scrollable (a blanket `overflow:visible` there used to push the page sideways on Timelines). Verify at real phone width with a throwaway `web/public/__preview.html` holding a 390px `<iframe src="/">` — the Chrome tool's `resize_window` does NOT change the page viewport on this machine; delete the file after.
- **Deploy recipe:** push to `main` → Vercel auto-builds. Vercel project MUST have **Root Directory = `web`** and framework Next.js (repo-root `main.py` otherwise triggers Python autodetect and fails; `web/vercel.json` pins framework). Private repo is fine.
- **Data-sync rule:** the app serves data COMMITTED under `web/public/{data,logos}` (self-contained — Vercel can't see repo root). **After every enrichment run `python graph_build.py --sync` (= graph + derive.py views + `npm run sync`) and commit `graph/` + `web/public`.** `sync-data.mjs` prefers the graph-derived files (`graph/company_metrics.json`, `graph/capex_backlog.json`, `graph/timelines.bundle.json`) over the root curated baselines when they exist. The predev/prebuild hook re-syncs locally and silently keeps the committed snapshot when sources are absent (as on Vercel). Which tabs enrichment feeds (verified 2026-08-27): Graph / Chain 2D / Coverage read the graph directly; Generations is graph-derived but only reacts to a company joining/leaving a generation chain (not to new signals); Timelines / Screener / Capex come from derive.py tags — see [[feedback_transcript_command]].
- **Quotes:** non-US tickers via `/api/quote` (Yahoo chart-API proxy; day-change from the 1d meta's previousClose, NOT a wider range's chartPreviousClose); US via TradingView widget.
- **User decision (2026-07-01): NO Quant tab in the web app.** Don't port it or re-offer.
- **Generations tab (2026-07-01)** = the transition delta engine (`web/src/lib/transitions.ts`): 4 transitions (B200→Vera Rubin, MI355→MI450/Helios, Trainium2→3, TPU v7→v8t+v8i) computed from node `chains[]`+`products[]` into retained/gained/lost buckets with product deltas (HBM3E→HBM4 etc.) + a "Generation exposure" section in NodePanel. If a NEW generation chain file is added later, extend `TRANSITIONS` there.
- **Coverage tab (2026-07-01)** = earnings calendar + enrichment queue: `/api/earnings` batches Yahoo v7 quote earnings dates (cookie+crumb handshake, 6h module cache); status per company = 🔴 Enrich now (call happened, lastData < call date) / 🗓 Upcoming / ✅ Covered; 📋 button copies `Transcript:<company>`. `web/src/lib/yahoo.ts` = shared (ticker,exchange)→Yahoo-symbol mapper w/ exchange suffix table (KOSDAQ=.KQ not .KS, XETRA=.DE, .PA/.AS/.VI/.L/.ST/.OL/.SW/.SZ); /api/quote uses it too.
- **Pipeline cards in Coverage (2026-09-11, user request "enrich calendar에 각 파이프라인이 언제 됐고 뭐가 부족한지"):** `enrich_status.py` (run by `graph_build.py` after evidence) attributes every graph label to a pipeline via `verify_graph.resolve_label` (folder of the resolved file: av/edgar/dart+supply_contracts/investing/tw/conferences, else manual) and writes `graph/enrich_status.json` → synced to `web/public/data` → Coverage.tsx renders one card per pipeline (last sync from state files, last enriched from patches/applied mtimes, source date range, pending queue list, saved-but-no-data list, edgar intentional skips, av waiting, tw no_media, conference runs[]). `enrich_log.json` (repo root, committed) is the append-only run record (one row per day+pipeline when counts change). Never hand-edit graph/enrich_status.json.
- **"Semi Bot" tab (2026-09-11, user request):** 10th tab after Ask in `page.tsx` embeds the user's OTHER Vercel project https://semiband-dashboard.vercel.app (title "Semi Bot") inline via `<iframe class="embed-frame">` (site sends no X-Frame-Options / CSP, checked with curl) plus an "Open in a new tab ↗" link. Constant `SEMI_BOT_URL` in page.tsx. This app's own Vercel project is `reticulum-ai` (web/.vercel/project.json).
- **UI workspace refresh (2026-09-12, Codex Astra xhigh):** `page.tsx` + `workspace.css` add research-view context, graph summary, keyboard-operated one-row scrolling tabs, load retry and empty-filter recovery. Sidebar sections/legend are native buttons; the mobile drawer traps focus, Escape closes it and restores focus, and its closed contents are hidden from keyboard navigation. SearchBox supports `/` to focus and keyboard activation of Clear. All text stays English. **Counter gotcha:** the force renderer mutates `viz.links[].source/target` from strings into node objects; display totals must use untouched `graph.edges` or normalize endpoints. No generated data or pipeline files changed. Desktop/phone verification and screenshots: `C:\Users\calif\Documents\Codex\2026-09-12\earnings-ui-review\`; deployment status in `docs/HANDOFF.md` latest entry. The user authorized immediate deployment for this session only; standing no-auto-commit preference is unchanged.
- Approved-in-principle next feature: ③ Ask-the-Graph chat (Claude API grounded ONLY in stored signals/contracts w/ source citations; needs ANTHROPIC_API_KEY env var in Vercel). News automation stays rejected per [[feedback-transcript-only-purism]]. See also [[project-state]], [[feedback-english-ui]].
